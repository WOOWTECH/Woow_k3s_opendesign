# Negative fixture: create: true while enabled: false would render a Secret the
# Deployment never reads. MUST fail.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  extraEnvSecret:
    enabled: false
    create: true
    data:
      OPENROUTER_API_KEY: example-value-not-a-credential
