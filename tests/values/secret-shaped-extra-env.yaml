# Negative fixture: a credential-shaped key in extraEnv (which renders into a
# cleartext ConfigMap) while the guard is on. MUST fail.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  rejectSecretShapedExtraEnv: true
  extraEnv:
    SOME_PROVIDER_API_KEY: example-value-not-a-credential
