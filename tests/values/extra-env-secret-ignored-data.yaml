# Negative fixture: data filled in while create is false. Nothing would render
# it, so those values would silently never reach the container. MUST fail.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  extraEnvSecret:
    enabled: true
    create: false
    data:
      OPENROUTER_API_KEY: example-value-not-a-credential
