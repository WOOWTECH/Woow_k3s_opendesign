# Negative fixture: the same key in extraEnv (cleartext ConfigMap) and in the
# Secret. envFrom order would silently decide the winner. MUST fail.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  extraEnv:
    OPENROUTER_API_KEY: example-value-not-a-credential
  extraEnvSecret:
    enabled: true
    create: true
    data:
      OPENROUTER_API_KEY: example-value-not-a-credential
