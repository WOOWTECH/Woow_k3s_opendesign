# Negative fixture: the chart is told to CREATE the extra-env Secret but given
# nothing to put in it. MUST fail rather than render an empty Secret.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  extraEnvSecret:
    enabled: true
    create: true
    data: {}
