# Negative fixture: a key with an empty value. required() MUST fire rather than
# hand the daemon an empty variable.
publicUrl: https://od-woow-k3s.woowtech.io
opendesign:
  extraEnvSecret:
    enabled: true
    create: true
    data:
      OPENROUTER_API_KEY: ""
